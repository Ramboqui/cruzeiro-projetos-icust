import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.projetounicsul.com',
  appName: 'ICust',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
